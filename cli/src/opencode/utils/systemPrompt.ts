/**
 * Automatic title instructions are intentionally disabled.
 * Manual title changes remain available through the MCP tool.
 */
export const TITLE_INSTRUCTION = '';

/**
 * The system prompt to inject for OpenCode sessions.
 */
export const opencodeSystemPrompt = TITLE_INSTRUCTION;
